{
  "name": "luminary-landing",
  "version": "1.0.0",
  "description": "Luminary SaaS Landing Page",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
